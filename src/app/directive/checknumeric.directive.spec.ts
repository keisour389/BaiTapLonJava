import { ChecknumericDirective } from './checknumeric.directive';

describe('ChecknumericDirective', () => {
  it('should create an instance', () => {
    const directive = new ChecknumericDirective();
    expect(directive).toBeTruthy();
  });
});
