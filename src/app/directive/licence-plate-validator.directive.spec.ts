import { LicencePlateValidatorDirective } from './licence-plate-validator.directive';

describe('LicencePlateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LicencePlateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
